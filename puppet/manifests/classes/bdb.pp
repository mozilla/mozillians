# NOTE: BELOW IS DOCS ONLY... I ended up doing this in the VM and then packaging
# Berkeley DB Support
class bdb {
/*
    Exec {
      path => [
          '/usr/local/bin',
          '/opt/local/bin',
          '/usr/bin', 
          '/usr/sbin', 
          '/bin',
          '/sbin'],
      logoutput => true,
      }

      exec {'wget http://download.oracle.com/berkeley-db/db-4.7.25.tar.gz':}
      exec {'tar zxf db-4.7.25.tar.gz':}
      exec {'cd db-4.7.25':}
      exec {'cd build_unix':}
      exec {'../dist/configure --bindir=/usr/local/bin --libdir=/usr/lib/ --includedir=/usr/include/ --enable-cxx':}
      exec {'make':}
      exec {'make install':}
      exec {'cd ../..':}
*/
}

